export interface SeedingPassengerInviteType {
    id: string
    userId: string
    orderId: string
}

export interface SeedingRidderInviteType {
    id: string
    userId: string
    orderId: string
}